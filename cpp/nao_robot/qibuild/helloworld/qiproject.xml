<project version="3">

  <qibuild name="rapp">
    <!-- Add your project dependencies here
    <depends buildtime="true" runtime="true" names="foo" />
    -->
    <depends buildtime="true" runtime="true" names="rapp-api" />
 </qibuild>

</project>
